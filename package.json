{
  "name": "latte-lang",
  "publisher": "smuuf",
  "displayName": "Latte",
  "description": "Support for Latte (Nette) template files.",
  "version": "0.0.1",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "engines": {
    "vscode": "^1.82.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "latte",
        "aliases": [
          "Latte",
          "latte"
        ],
        "extensions": [
          ".latte"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "latte",
        "scopeName": "source.latte",
        "path": "./syntaxes/latte.tmLanguage.json"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Example configuration",
      "properties": {
        "latte-lsp.trace.server": {
          "scope": "window",
          "type": "object",
          "description": "Traces the communication between VS Code and the language server.",
          "properties": {
            "verbosity": {
              "type": "string",
              "description": "Controls the verbosity of the trace.",
              "enum": [
                "off",
                "message",
                "verbose"
              ],
              "default": "verbose"
            },
            "format": {
              "type": "string",
              "description": "Controls the output format of the trace.",
              "enum": [
                "text",
                "json"
              ],
              "default": "text"
            }
          }
        }
      }
    }
  },
  "capabilities": {
    "hoverProvider": "true"
  },
  "devDependencies": {
    "@swc/core": "^1.3.90",
    "@swc/jest": "^0.2.29",
    "@types/jest": "^29.5.5",
    "@types/node": "^20.6.2",
    "@types/vscode": "^1.82.0",
    "jest": "^29.7.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.0"
  }
}
